# wrangler CLI로 배포할 경우의 설정 (대시보드 붙여넣기 배포 시 불필요)
#   npx wrangler deploy
#   npx wrangler secret put ANTHROPIC_API_KEY
#   npx wrangler secret put ACCESS_PASSWORD
name = "zenji-proxy"
main = "worker.js"
compatibility_date = "2026-09-01"

[vars]
ALLOWED_ORIGINS = "https://kimdoyun0806.github.io,http://localhost:5173,http://localhost:4173"
